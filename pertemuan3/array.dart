void main(List<String> args) {
    var liststring = <String>["AMCC","Satu","Tiga","Empat"];
    List<int> angka = [1,2,3,4]; //bisa lebih dari dua data 
    
    angka.add(5); //hanya satu data dan untuk penambah list
    for (int i = 0; i < liststring.length; i++);

    print(liststring[4]);
    //print(angka);
  
}